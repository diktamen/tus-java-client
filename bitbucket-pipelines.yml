image: amazoncorretto:17-alpine-full
pipelines:
  default:
    - step:
        script:
          - apk update
          - apk add --no-cache bash shadow
          - bash ./gradlew --no-daemon build jar
          - pwd
          - find / -iname "*.jar"
        artifacts: # defining the artifacts to be passed to each future step.
          - ~/build/libs/*.jar
